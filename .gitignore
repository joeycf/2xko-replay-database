.env
node_modules
raw/

# Nuxt / build artifacts
.nuxt
.output
.vercel
.data
dist
public/data/
cache/

# Local agent config
.claude/

# Local credentials — yt-dlp YouTube session cookies (see README, "YouTube
# session cookies"). A live Google session; must never be committed.
secrets/
